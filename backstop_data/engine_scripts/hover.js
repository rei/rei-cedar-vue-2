module.exports = function (chromy, scenario, vp) {
  console.log('>>>>>>');
  // console.log(chromy.rect(scenario.selectors[0]));

  // chromy.select(scenario.selectors[0]);

  // .mouse.move(scenario.selectors[0]);
};
