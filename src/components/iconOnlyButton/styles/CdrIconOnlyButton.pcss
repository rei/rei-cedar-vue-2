/* ==========================================================================
  # CdrIconOnlyButton
  
  All values should map to variables in
  vars/CdrIconOnlyButton.vars.pcss
  in order to allow for theming

  TOC:

    :Base - CdrIconOnlyButton
========================================================================== */

.cdr-icon-only-button {
  background: none;
  border: none;
  cursor: pointer;
  display: inline-block;
  padding: $inset-quarter-x;

  & > .cdr-icon {
    fill: $heart-of-darkness;
  }

  &--on-dark {
    & > .cdr-icon {
      fill: $moon-shot;
    }
  }
}
