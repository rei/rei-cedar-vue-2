/* ==========================================================================
  # cdrIcon
  
  All values should map to variables in
  vars/cdrIcon.vars.pcss
  in order to allow for theming

  TOC:

    :Base Styles
========================================================================== */

.cdr-icon {
  width: $icon-width;
  height: $icon-height;
  fill: $icon-fill;
  flex: 0 0 auto;

  & path {
    all: inherit;
  }
}
