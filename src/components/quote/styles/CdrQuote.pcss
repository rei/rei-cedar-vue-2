/* ==========================================================================
  # Cdrquote
  
  TOC:

    :Base - Cdrquote
========================================================================== */
@import './spruceQuote.css';

.cdr-quote {
  margin: 0;
  padding: $inset-1-x-stretch;
  
  &__summary {
    font-size: 20px;
    font-style: italic;
    font-family: Sentinel, Roboto, "Helvetica Neue", Helvetica, Arial, sans-serif;
    font-weight: 400;
    line-height: 28px;
    letter-spacing: 0;
    margin: 0;

    @media (--sm-mq) {
      font-size: 24px;
      line-height: 32px;
    }

    & + .cdr-quote__citation {
      display: block;
      padding-top: $space-1-x;
    }
  }

  &--pull {
    border-style: solid;
    border-color: #222;

    .cdr-quote__summary {
      font-weight: 600;
    }

    @media (--xs-mq) {
      border-width: 0 0 1px 0;
      padding: $inset-1-x-stretch;
      margin: 0 0 $space-1-x;
    }

    @media (--sm-mq) {
      border-width: 0 0 0 1px;
      padding: $space-half-x $space-1-x $space-half-x $space-2-x;
      margin: $space-1-x 0;
    }
  }
}
