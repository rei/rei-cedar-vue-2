import { shallow } from '@vue/test-utils';
import CdrTab from 'Components/tab/CdrTab';

describe('CdrTab.vue', () => {
  it('has a failing test by default so you remember to do them', () => {
    expect(false).toBe(true);
  });
});