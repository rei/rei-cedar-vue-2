<cedar-card-image
    data-={ opts.option }
    data-display-position={opts.position}
    data-display-ratio={ opts.ratio }
    data-display-orientation={ opts.orientation }
    data-display-alignment={ opts.alignment }
    >
    <img src={opts.url} alt={opts.alt} />
</cedar-card-image>
